# Raw datasets (large, downloadable — see data/README.md for sources)
data/raw/*.mtx
data/raw/*.zip

# Generated artifacts -- GraphML snapshots are regenerable and voluminous
# (100+ files,100+MB); videos are the actual required deliverable and are
# small (a few MB total), so those ARE committed.
outputs/**/*.graphml

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Reference materials only -- not part of this project's own deliverable
# (course spec PDF; a friend's separate shortest-path project notebook used
# only as a pipeline-pattern reference, see README.md "Acknowledgements")
CSAI335-Summer-2026-Course-Projects-ver12*.pdf
Final_Alg_Proj*.ipynb
